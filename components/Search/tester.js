  // const [show, setShow] = useState(false);
  // const handleClose = () => setShow(false);
  // const handleShow = () => setShow(true);
  // const [name,setName] = useState(data.map(item => item.name));
  // const [profile,setProfile] = useState(data.map(item => item.description.profile))
  // const[fee,setFee] = useState(data.map(item => item.description.fee));
  // const[job,setJob] = useState(data.map(item => item.description.job));
  // const[language,setLanguage] = useState(data.map(item => item.description.language ));
  // const[experience,setExperience] = useState(data.map(item => item.description.experience ));
  {/* <Modal
        show={show}
        onHide={handleClose}
        backdrop="static"
        keyboard={false}>
        <Modal.Header>
          <Modal.Title>Update Profile</Modal.Title>
        </Modal.Header>
        <Modal.Body>
          <label>
            Name*
          </label>
          <FormControl type="text" placeholder="Enter your name" onChange={e => setName(e.target.value)} />
          <label>
            Fee*
          </label>
          <FormControl type="tel" placeholder="Enter your new fee" onChange={e => setFee(e.target.value)} />
          <label>
            Designation*
          </label>
          <FormControl type="text" placeholder="Enter your updated designation" onChange={e => setJob(e.target.value)} />
          <label>
            Language*
          </label>
          <FormControl type="text" placeholder="Enter your updated Languages" onChange={e => setLanguage(e.target.value)} />
          <label>
            Experience*
          </label>
          <FormControl type="tel" placeholder="Enter your updated Experience" onChange={e => setExperience(e.target.value)} />
          <label>
            Qualification*
          </label>
          <FormControl type="text" placeholder="Enter your updated qualifications" onChange={e => setProfile(e.target.value)} />
          
          <Form.File
              className="position-relative"
              required
              name="file"
              label="File"
          />
        </Modal.Body>
        <Modal.Footer>
          <Button variant="secondary" onClick={handleClose}>
            Close
          </Button>
          <Button variant="primary"onClick={handleClose}>Update</Button>
        </Modal.Footer>
      </Modal> */}